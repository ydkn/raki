<% if diff.lines.empty? %>
  <%=t 'diff.no_diff' %>
<% else %>
  <table class="diff">
    <% diff.lines.each do |diff_line| %>
      <tr>
        <td class="line <%=h diff_line.type %>"><%=h diff_line.line %></td>
      </tr>
    <% end %>
  </table>
<% end %>